#
# Configuration settings that Mocha will use internally.
# 

search_dirs: "Mocha"
pages_dir: "pages"
partials_dir: "pages/partials"
site_url: "localhost:5000"
site_title: "Default Mocha Build"
build_dir: "site"
assets_dir: "assets"
